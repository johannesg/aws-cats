type User {
    id: ID!
    email: String!
}

type Cat {
    id: ID!
    url: String!
    height: Int
    width: Int
}

type Cats {
    random(pageSize: Int = 10): [Cat]
}

type Query {
    cats: Cats
    me: User
    hello: String
}
