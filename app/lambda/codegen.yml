schema: ./schema.ts

generates:
  types/gen-types.ts:
    config: 
      # defaultMapper: any
      showUnusedMappers: true
      contextType: ./#Context
      useIndexSignature: true
    plugins: 
      - typescript
      - typescript-resolvers